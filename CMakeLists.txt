cmake_minimum_required (VERSION 2.6)
project (cc-opencv)
find_package (OpenCV REQUIRED)
include_directories (${OpenCV_INCLUDE_DIRS})
add_executable (cc-opencv main.cpp)
target_link_libraries (cc-opencv ${OpenCV_LIBS} "-static")